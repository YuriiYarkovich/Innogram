export enum FollowAcceptedStatus {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  REQUESTED = 'requested',
}

export enum PostStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  DELETED = 'deleted',
}

export enum MessageVisibilityStatus {
  ACTIVE = 'active',
  EDITED = 'edited',
  DELETED = 'deleted',
}

export enum MessageReadStatus {
  READ = 'read',
  UNREAD = 'unread',
}

export enum AssetType {
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum ChatTypes {
  PRIVATE = 'private',
  GROUP = 'group',
}

export enum ChatStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  DELETED = 'deleted',
}

export enum ChatParticipantRole {
  PARTICIPANT = 'participant',
  ADMIN = 'admin',
}
