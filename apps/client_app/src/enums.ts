export enum FollowAcceptedStatus {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  REQUESTED = 'requested',
}

export enum PostStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  DELETED = 'deleted',
}
