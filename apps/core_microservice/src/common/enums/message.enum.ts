export enum MessageVisibilityStatus {
  ACTIVE = 'active',
  EDITED = 'edited',
  DELETED = 'deleted',
}

export enum MessageReadStatus {
  READ = 'read',
  UNREAD = 'unread',
}

export enum AssetType {
  IMAGE = 'image',
  VIDEO = 'video',
}
