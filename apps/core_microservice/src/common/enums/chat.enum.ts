export enum ChatTypes {
  PRIVATE = 'private',
  GROUP = 'group',
}

export enum ChatStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  DELETED = 'deleted',
}

export enum ChatParticipantRole {
  PARTICIPANT = 'participant',
  ADMIN = 'admin',
}
